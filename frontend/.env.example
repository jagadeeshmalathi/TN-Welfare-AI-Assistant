# Frontend configuration — copy to .env.local for local overrides.

# Backend API base URL.
# - Leave UNSET in local dev: Vite proxies "/api" -> http://localhost:8000.
# - In production (Vercel), set this to your deployed backend, e.g.
#   VITE_BACKEND_URL=https://tn-welfare-backend.up.railway.app
# VITE_BACKEND_URL=
